/*
  Toolbar style

  -- toolbar are components that handle actions for their parent context
*/
.toolbar
    .btn
        border      none
        color       basecolor
        text-shadow none
        box-shadow  none

        &:hover
            color actioncolor

    .fa-compress
        transform none


.dropdown-menu
    for num in (1..10)
        .pusher-{num}
            padding-left num * 20px + 20px
