// Generated by CoffeeScript 1.8.0
var ImapHelper, ImapProcess, Promise;

ImapHelper = require('./helper');

Promise = require('bluebird');

module.exports = ImapProcess = {};

ImapProcess.getMailboxes = function(account) {
  var pConnection;
  pConnection = ImapHelper.getConnection(account);
  return Promise.using(pConnection, function(connection) {
    return connection.getBoxesAsync().then(ImapHelper.cleanUpBoxTree);
  });
};

ImapProcess.checkForUpdates = function(account) {};
