// Generated by CoffeeScript 1.8.0
async.timeoutProtect = function(ms, callback) {
  var error, first, newCallback, timeout;
  error = new TimeoutError();
  first = false;
  timeout = setTimeout(function() {
    if (first) {
      callback(error);
    }
    return first = false;
  }, ms);
  return newCallback = function() {
    clearTimeout(timeout);
    if (first) {
      callback.apply(null, arguments);
    }
    return first = false;
  };
};

async.delay = function(ms, fn) {
  return setTimeout(fn, ms);
};
