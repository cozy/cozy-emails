// Generated by CoffeeScript 1.8.0
var americano;

americano = require('americano');

module.exports = {
  account: {
    all: americano.defaultRequests.all,
    pathById: function(doc) {
      var emitChildren;
      return (emitChildren = function(children) {
        var child, _i, _len, _ref;
        for (_i = 0, _len = children.length; _i < _len; _i++) {
          child = children[_i];
          emit(child.id, child.path);
          if ((_ref = child.children) != null ? _ref.length : void 0) {
            emitChildren(child.children);
          }
        }
      })(doc.mailboxes);
    }
  },
  message: {
    all: americano.defaultRequests.all,
    byMailboxAndDate: function(doc) {
      var boxid, _i, _len, _ref, _results;
      _ref = doc.mailboxIDs;
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        boxid = _ref[_i];
        _results.push(emit([boxid, doc.createdAt], null));
      }
      return _results;
    }
  }
};
